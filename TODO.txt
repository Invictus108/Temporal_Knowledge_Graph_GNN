Current Tasks:
Tony: get classes for entities
Victoria: research and refined model 
Jaden: generate dataset
Nevin: look into loss functioncs


add classes to dataset
generate embedding vectors for dataset (text_emb + class_emb)

convert dataset to pytorch geometric 
    - input (graph at t), target (graph at t+1)
      - MSE between adj matrix?
      - other graph similarity metrics?
    - input (graph at time t), target (changes between graph t and t+1)
       - order invariant?
       - irregular?


model output:
  - entire graph at t+1
  - changes to graph t to get t+1


define model (link pred) 
   - attention conv
   - poolings (attention or diffpool)
   - lstm layers 
   - candidate selection
   - loss (research graph similarity metrics)

train and tune


Problems:
graph is actaully a bunch of subgraphs
   - this is ok
   - if anything this will just help the model
nodes are appearing and vanishing
   - create a global node set that is same across all steps
   - when there are no edges connecting its just alone

