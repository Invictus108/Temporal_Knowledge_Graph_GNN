to much of the model depends on slow CPU operations
need to find a way to make it more parallel to take advantage of GPUs
or use a bunch of memonization to take advantage of lots of RAM


Progress:
Tried full graph prediction by giving it entire graph + sampled additions
   - massive pos neg imbalance, just learned to pass on everything

Sampling from current edges as positives, equal amount additions as negatives
   - basically first version but with balanced pos-neg
   - it seemed to be able to tell why is from current graph and which is additon and passed only current

Deltas
  - made two scoring heads one for additions, one for deletions
      - deletions consist of edges that are actaully being removed + equal # sampled cur triplets
      - additions are triplets being added + equal # proposals from candidate proposal strategies
  - Dosent seem to work

Deltas with rel time v6:
   - added time a edge has been alive to the model and had a agent refactor the entire code base
   - del actually appears to work on syntheic data, getting perfect F1 on toy dataset

Deltas with rel time and history awarness (deltas_v7):
   - same as v6, but we now track node, pair, and triplet history to build a strong understanding of whats makes a valid triplet
   - used to both make better candidate proposals and for scoring additions


Next:
Add more noise to training?
